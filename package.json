{
  "name": "tinycloud-node",
  "version": "0.2.0",
  "private": true,
  "description": "TinyCloud Protocol Node - Rust server with changesets for versioning",
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version",
    "release": "cargo build --release"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1"
  }
}
